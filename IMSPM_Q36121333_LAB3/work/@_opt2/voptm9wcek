library verilog;
use verilog.vl_types.all;
entity top_dither_deadtime is
    port(
        clk             : in     vl_logic;
        rst             : in     vl_logic;
        dither_in       : in     vl_logic_vector(9 downto 0);
        duty_high       : out    vl_logic;
        duty_low        : out    vl_logic
    );
end top_dither_deadtime;
